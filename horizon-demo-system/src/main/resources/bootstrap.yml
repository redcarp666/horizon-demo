server:
  port: 9001
  servlet:
    context-path: /system

spring:
  application:
    name: SYSTEM-SERVICE
  profiles:
    active: dev   # local 本地 dev:开发环境 test：测试环境  uat:uat环境 prod:生产环境
  jta:
    log-dir: ../logs/system